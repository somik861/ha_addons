name: "mopidy-speaker"
description: "Mopidy installation tuned to be a speaker for Jellyfin"
version: "1.0.0"
slug: "mopidy-speaker"

arch:
  - aarch64
  - amd64
audio: true
startup: application
host_network: true
map:
  - config:rw
devices:
  - /dev/snd
